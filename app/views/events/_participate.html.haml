- if @event.players.exists?(:id => current_user)
  = link_to content_tag(:i,'',class: 'block ss-deleteuser'), event_participant_path(@event), confirm: 'Are you sure?', method: :delete, class: 'button small expand radius alert'
- else
  - if logged_in?
    = link_to content_tag(:i,'',class: 'block ss-adduser'), event_participants_path(@event), method: :post, class: 'button small expand radius success'
  - else
    = link_to content_tag(:i,'',class: 'block ss-power'), "/auth/twitter", method: :post, class: 'button small expand radius success'
